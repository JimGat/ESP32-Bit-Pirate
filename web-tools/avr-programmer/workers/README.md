# Legacy worker directory

The published AVRDUDE module creates `new Worker("avrdude-worker.js")` relative
to the tool page, so the runtime worker must live at
`avr-programmer/avrdude-worker.js`. This directory is intentionally retained
only to document the previous prototype layout.
